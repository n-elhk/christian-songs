import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.christian.songs',
  appName: 'christian-songs',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
